
PORT=5001
NODE_ENV=development

DATABASE_URL="postgresql://postgres:YPglHJFjvbJEHB3hrDZq@187.53.129.92:5330/tizellodb?schema=public"
# DATABASE_URL=postgresql://postgres:YPglHJFjvbJEHB3hrDZq@development-tizello-fwo7mx:5432/tizellodb
# Redis, used by both the app cache and BullMQ.
# REDIS_URL="redis://default:si5cxvnb3znem11n@development-tizellocache-hvjyz3:6379"
REDIS_URL="redis://localhost:6379"



JWT_SECRET="348f4ffd4c3e72985150ffc0e3903c556944848bf11a64570f91d18a327fe4a5c00b19095bb938a9dd2cde29fd4594f4"
JWT_EXPIRY="50m"

# Browser origin allowed through CORS. Optional — defaults to "*".
CLIENT_ORIGIN="http://localhost:3000"


# Optional — defaults to "debug" in development and "info" otherwise.
LOG_LEVEL="debug"

HOST_MAIL="taufikislam172@gmail.com"
HOST_APP_PASSWORD="otwj kcnj ksbs hvtv"


# only. Setting secure=true on 587 hangs the connection rather than erroring.
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_SECURE=false


# MAIL_FROM address would be silently ignored.
MAIL_FROM_NAME="Tizello"


# The refresh token is opaque random bytes, not a JWT, so it needs no secret.
REFRESH_TOKEN_TTL_DAYS=30


# safe, lowering it is not.
BCRYPT_COST=12

# ─── Auth: login codes (plan §3.4) ──────────────────────────────────────────
# the length. Both are enforced on the LoginCode row, not in Redis.
LOGIN_CODE_TTL_MINUTES=10
LOGIN_CODE_MAX_ATTEMPTS=5

# ─── Auth: registration verification & password reset ───────────────────────
REGISTRATION_CODE_TTL_MINUTES=5
REGISTRATION_CODE_MAX_ATTEMPTS=5
PASSWORD_RESET_TTL_HOURS=1

# ─── Invitations (plan §3.6, §8) ────────────────────────────────────────────
# 7 days must match the shipped frontend copy: "Invitation links last seven
# days." Change one and you must change the other.
INVITE_TTL_DAYS=7
# Per-workspace hourly cap. An admin mass-inviting sends mail from our domain
# to strangers — this is the spam-reputation guard, not an abuse counter.
INVITE_MAX_PER_WORKSPACE_HOUR=50

# ─── OAuth 2.0 client credentials ───────────────────────────────
GOOGLE_CLIENT_ID="657073902718-sqqq9787polliui4h29ol4qr60mrmknl.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="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"
GOOGLE_CALLBACK_URL="https://development-tizzellobackend-srvfzj-6c803d-187-53-129-92.sslip.io/api/v1/auth/google/callback"

#github oauth 
GITHUB_CLIENT_ID="Ov23li8ULVn8PRJUwfDM"
GITHUB_CLIENT_SECRET="967e38444cda461491df238e155de3a96d630eb7"
GITHUB_CALLBACK_URL="https://development-tizzellobackend-srvfzj-6c803d-187-53-129-92.sslip.io/api/v1/auth/github/callback"


# --- uploads (project FILES properties) ---
UPLOAD_DIR="uploads"
UPLOAD_MAX_BYTES=10485760
